# Commands

> Cheat-sheet of commands a contributor (human or AI) actually runs in this repo. One line per command, real flags. Keep it short — only the ones that matter.

## Install

```
# language deps (e.g. bundle install, pnpm install, uv sync, go mod download)
```

## Dev / run locally

```
# how to start the app locally (e.g. bin/rails server, pnpm dev, tofu plan)
```

## Lint / format

```
# linter + autoformatter
```

## Test

```
# all tests
# focused single-file test
# focused single-test test
```

## Build

```
# production build (if relevant)
```

## Migrate / seed (if applicable)

```
# create + apply migrations, seed dev data
```

## Deploy

```
# what triggers production deploy (e.g. push to main → Coolify, gh workflow run)
```

## Debug helpers

```
# console / shell into running container
# follow logs
# inspect queue / jobs
```
