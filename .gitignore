# User-specific configuration generated by install.sh.
# Contains local defaults; not for version control.
aidw.env.sh

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Local workflow state (covered by global gitignore, but belt-and-suspenders)
.wip/
.claude/repo-docs/
.claude/settings.local.json

# Locally installed Go (auto-installed by install.sh if Go not found)
.go/
